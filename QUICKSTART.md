# Быстрый старт

## 1️⃣ Скачивание репозитория

```bash
# Клонировать репозиторий
git clone https://github.com/AlexRokalo/stockcheker.git

# Перейти в папку проекта
cd stockcheker

# Переключиться на ветку с кодом
git checkout claude/stock-analysis-system-011CUoN1uYS43Aa955UYDVJf
```

## 2️⃣ Установка зависимостей

### Вариант 1: С виртуальным окружением (рекомендуется)

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать виртуальное окружение
# На Linux/Mac:
source venv/bin/activate
# На Windows:
# venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### Вариант 2: Без виртуального окружения

```bash
# Просто установить зависимости
pip install -r requirements.txt
```

## 3️⃣ Запуск анализа

### Простой пример - анализ Apple

```bash
python main.py --ticker AAPL
```

### Анализ с периодом 6 месяцев

```bash
python main.py --ticker GOOGL --period 6mo
```

### Анализ нескольких акций

```bash
python main.py --tickers AAPL GOOGL MSFT TSLA
```

### Быстрый пример (готовый скрипт)

```bash
python example.py
```

## 4️⃣ Просмотр результатов

После запуска графики будут сохранены в папке `graphs/`:

```bash
# Посмотреть список созданных файлов
ls -l graphs/

# Открыть папку с графиками (на Mac)
open graphs/

# На Linux
xdg-open graphs/

# На Windows
explorer graphs
```

## 5️⃣ Настройка Google Sheets (опционально)

Если хотите использовать Google Sheets:

### Шаг 1: Получить credentials

1. Перейти в [Google Cloud Console](https://console.cloud.google.com/)
2. Создать проект
3. Включить Google Sheets API
4. Создать Service Account
5. Скачать JSON ключ и сохранить как `credentials.json` в корне проекта

### Шаг 2: Подготовить таблицу

Создайте Google Sheets таблицу с такой структурой:

| Ticker | Название (опционально) |
|--------|------------------------|
| AAPL   | Apple Inc.             |
| GOOGL  | Alphabet Inc.          |
| MSFT   | Microsoft Corp.        |

### Шаг 3: Дать доступ

1. Открыть `credentials.json`
2. Найти поле `client_email`
3. В Google Sheets нажать "Share"
4. Добавить этот email с правами редактора

### Шаг 4: Запустить анализ

```bash
# Замените YOUR_SHEET_ID на ID вашей таблицы
# ID находится в URL: https://docs.google.com/spreadsheets/d/YOUR_SHEET_ID/edit
python main.py --sheet YOUR_SHEET_ID
```

## 📊 Примеры команд

```bash
# Анализ Tesla за 1 год (по умолчанию)
python main.py --ticker TSLA

# Анализ Microsoft за 3 месяца
python main.py --ticker MSFT --period 3mo

# Сравнение tech-гигантов
python main.py --tickers AAPL GOOGL MSFT AMZN META

# Только анализ, без графиков (быстрее)
python main.py --ticker AAPL --no-visualize

# Из Google Sheets
python main.py --sheet 1AbCdEfGhIjKlMnOpQrStUvWxYz

# Помощь по командам
python main.py --help
```

## 🎯 Доступные периоды

- `1d` - 1 день
- `5d` - 5 дней
- `1mo` - 1 месяц
- `3mo` - 3 месяца
- `6mo` - 6 месяцев
- `1y` - 1 год (по умолчанию)
- `2y` - 2 года
- `5y` - 5 лет
- `10y` - 10 лет
- `ytd` - С начала года
- `max` - Максимум

## 🔧 Устранение проблем

### Ошибка: command not found: python

```bash
# Попробуйте python3 вместо python
python3 main.py --ticker AAPL
```

### Ошибка: No module named 'yfinance'

```bash
# Переустановите зависимости
pip install -r requirements.txt

# Или вручную
pip install yfinance pandas matplotlib seaborn plotly ta
```

### Ошибка: Permission denied

```bash
# Сделайте файлы исполняемыми
chmod +x main.py example.py

# Или запускайте через python
python main.py --ticker AAPL
```

### Проблемы с Google Sheets

```bash
# Проверьте, что файл credentials.json существует
ls -la credentials.json

# Проверьте права доступа к таблице (email из credentials.json должен иметь доступ)
```

## 📁 Структура выходных файлов

После запуска в папке `graphs/` будут созданы:

```
graphs/
├── AAPL_price_volume.png        # График цены и объема
├── AAPL_technical.png           # Технические индикаторы
├── AAPL_candlestick.html        # Интерактивный график (откройте в браузере)
├── AAPL_report.png              # Полный отчет
└── comparison.png               # Сравнение (если несколько акций)
```

## ⚡ Самый быстрый старт (один скрипт)

Если хотите просто быстро попробовать:

```bash
# Все в одном
git clone https://github.com/AlexRokalo/stockcheker.git && \
cd stockcheker && \
git checkout claude/stock-analysis-system-011CUoN1uYS43Aa955UYDVJf && \
pip install -r requirements.txt && \
python main.py --ticker AAPL
```

Готово! Графики будут в папке `graphs/` 🎉
